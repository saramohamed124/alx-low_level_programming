#include "main.h"
/**
 * _isupper: checks for uppercase chracters
 *@c: the characters 
 * Return: 1 if c is upper and 0 if otherwis
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
