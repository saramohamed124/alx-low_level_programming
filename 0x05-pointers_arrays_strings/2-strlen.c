#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s : it is a string which will be calculated
 * Return: return  strl the calculation (length) of string of @s
 */
int _strlen(char *s)
{
return (strlen(s));
}
